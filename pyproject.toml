[[project]
name = "telegram-daddy"
version = "0.1.0"
description = "Telegram bot"
requires-python = ">=3.12"
dependencies = [
    "workers-runtime-sdk"
]

[dependency-groups]
dev = [
    "workers-py"
]
