[project]
name = "telegram-daddy"
version = "0.1.0"
description = "Telegram bot"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
